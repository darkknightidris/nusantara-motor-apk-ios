# 06 — Log Sesi

Format: `| tanggal | sesi | ringkasan | langkah berikutnya |`

| Tanggal | Sesi | Ringkasan | Langkah Berikutnya |
|---|---|---|---|
| 2026-09-10 | 1 | Audit project lama (web: FastAPI+Supabase+Next16; PWA: Next16.3.4 static export). Cek environment (Node 24, JDK 21, git, python, keytool ada; Android SDK belum). Verifikasi 9 tool harness + browser-use (bukti live: buka google.com & hackerone.com/hacktivity). Estimasi token/wall-clock dibuat. Struktur repo + sub-file dibuat. | Menunggu jawaban 7 pertanyaan + "eksekusi" → mulai T01 |
| 2026-09-10 | 2 | Scaffolding T01: 7/7 pertanyaan terjawab & dikunci di 01-SPEK; salin source PWA → repo (app/, components/, lib/, public/); hapus sw.js + ServiceWorkerRegister; next.config `output: "export"`; aturan disiplin checkpoint & anti-loop ditambahkan ke STATE.md; script T01 background ditulis. (Catatan sesi 3: script itu ternyata tak pernah tereksekusi — lihat 07-EROR #9) | T01 build |
| 2026-09-10 | 3 | Verifikasi ulang artefak T01: script background sesi 2 tak pernah jalan; scaffolding tak ter-commit. Fix + eksekusi ulang: commit scaffolding; 07-EROR #9–13; T01 **SELESAI** (next build → out/, cap init, Android SDK lengkap [cmdline-tools, platform-tools, platforms;android-35, build-tools;35.0.0], cap add, 3 plugin, cap sync). Bug di-fix: SWC korup (tarball resmi), route `[id]` → `?id=` (static export), manifest force-static, `.next` stale, `android.exe` menggantikan sdkmanager (semicolon), manual zip build-tools (SHA1 OK), wildcard `[id]` (LiteralPath). | Mulai T02 (lapisan DB lokal SQLite) |
| 2026-09-10 | 4 | T02 sub-task 1 (init DB + CREATE TABLE): audit API plugin @capacitor-community/sqlite 8.1.1 (options-object; `CapacitorSQLite` instance singleton — bukan class/connect()). `lib/schema.ts` (DDL 9 tabel + 9 indeks, murni data, sesuai 04-SCHEMA-DB.md) + `lib/db.ts` (ensureDb idempoten: open → PRAGMA foreign_keys → batch DDL → meta.db_version; helper run/qRows/qAll/tx + transaksi native; uuid/nowISO/deriveStatus; guard non-native). Skrip verifikasi `scripts/verify-schema.mjs` (node:sqlite built-in, import .ts langsung via type stripping Node 24): 30/30 PASS (DDL, 9 tabel, 9 indeks, FK cascade, CHECK, UNIQUE, CRUD, meta). `tsc --noEmit` bersih; `next build` OK (12 halaman static). Catatan: bug skrip tes (`.changes` milik hasil run(), bukan prepare) di-fix; typo FK guides→products di-fix. Sub-task 2 (seed user default): `ensureDb()` seed `mastaufiq`/`admin` role owner (keputusan terkunci #3 — menggantikan asumsi admin/admin123 di teks tugas), idempoten + self-healing bila username terhapus, `meta.seeded_at`. Sub-task 3 (hash): `lib/hash.ts` (sha256Hex WebCrypto primary + fallback js-sha256; genSalt 16 byte acak per user; hashPassword=SHA-256(salt+password)); `npm i js-sha256`; `scripts/verify-hash.mjs` 20/20 PASS (primary & fallback identik dgn node:crypto, salt unik x100, deterministik). Sub-task 4+6: `lib/localApi.ts` (±35KB) — semua signature api.ts PWA + CRUD transactions/payments/users/attachment dari web lama; xlsx (SheetJS) & pdf (jsPDF) di-npm, export via Filesystem→Share; role enforcement & validasi payload tiru backend; logika business (stock clamp, deriveStatus, recompute subtotal) tertanam. Bug fix: genSalt import ke atas, PRODUCT_COLS_ARR utk UPDATE, qRows utk join/subset-select, Blob utk writeFile, hapus mimeType di Share. `tsc` bersih, `next build` OK (12 halaman).  | T02 sub-task 5 (ganti import halaman api→localApi): `lib/localApi.ts` (signature penuh + CRUD transactions/users + attachment) |
| 2026-09-10 | 5 | T02 sub-task 5 selesai: 6 halaman app/(app) (dashboard, katalog, produk, tambah, penjualan, nota) + `lib/session.ts` (dynamic import login) kini import `@/lib/localApi`. Pola alias `import { localApi as api, ApiError } from "@/lib/localApi"` (destructuring `const { localApi: api }`) agar 21 call-site `api.*` tak berubah → diff minimal, stabil utk re-sync T03. Verifikasi semua method yang dipakai halaman ada di localApi (20/20: login, listSales, getTransactionSummary, getLowStock, listProducts, exportProductsExcel/Pdf, importProductsExcel, getGuidesByProduct, get/update/deleteProduct, create/update/deleteGuide, createProduct, get/deleteSale, createSale, exportSalesExcel). Komentari header dashboard & session.ts disesuaikan. `tsc --noEmit` bersih; `next build` OK (12 halaman); grep `@/lib/api` di app/ & lib/ = 0 sisa. `lib/api.ts` masih ada → dihapus di sub-task 7. | T02 sub-task 7: grep `fetch(`, `API_BASE`, `NEXT_PUBLIC_API_URL` → hapus `lib/api.ts` & env tak terpakai |
| 2026-09-11 | 7 | **T03 SELESAI** (FASE 3). Verifikasi E2E di browser (app :3100): app render — `/login` form (Username/Password/Masuk), banner "OFFLINE / LOKAL", nav 5 item (Beranda/Katalog/Jual/Keuangan/Profil); semua rute group `(app)` redirect ke `/login` saat belum sesi (`/dashboard`, `/keuangan`, `/panduan`, `/profil`, `/users` → `/login`, shell + nav tampil). `next build` → `out/` 18 rute; `tsc --noEmit` bersih; `lib/backup.ts` (190 baris) ter-commit. **Catatan arsitektur PENTING:** app = native Android/Capacitor; lapisan data (SQLite native) hanya jalan di dalam APK — di browser desktop hanya UI shell yang bisa diverifikasi; data (login, dashboard) butuh lingkungan Android native. T04 = seeding + backup/restore diuji di lingkungan native. | T04 (seeding + backup/restore) di lingkungan Android native |
| 2026-09-11 | 6 | Diagnosa app blank di :3100 (app :3100 tampil blank di browser). Akar: `RequireAuth` di `app/(app)/layout.tsx` membungkus SEMUA halaman group `(app)` termasuk `/login` → guard render `null` saat belum login → konten `<main>` kosong → blank. Dan `/login` ikut terwraps → blank juga → tak bisa login → app blank selamanya. Fix: `/login` jadi rute PUBLIK (`app/login/`) + `Shell` komponen bersama (OnlineStatus+main+BottomNav); group `(app)` = dashboard/katalog/penjualan/keuangan/panduan/profil/users (di-guard `RequireAuth`); hapus `app/(app)/login` (konflik rute). Verifikasi: `/login` render form (publik); semua rute 200 (dashboard/katalog/keuangan/tambah/detail/panduan/profil/users); konflik rute teratasi; shell + nav 5 item tampil. Commit `1bf8685`. Sisa T03: `lib/backup.ts` (backup/restore) + verifikasi end-to-end (login→dashboard) di browser + build final. | T03: verifikasi end-to-end di browser (login→dashboard) + `lib/backup.ts` + build final |
| 2026-09-12 | 9 | T07 (Beranda) — redesign hub ala BYON: grid aksi cepat (8 tile: Jual, Katalog, Keuangan, Panduan, Tambah, Hutang, Users, Backup) + header section + ringkasan data. Fitur signup ditemukan: `/daftar` (rute publik `app/daftar/`) + link "Daftar" di halaman login. Tambah halaman `app/(app)/dashboard/page.tsx` (BYON grid). Komitmen sesi: Beranda sebagai hub semua fitur. | Verifikasi build + pasang ke emulator + screenshot komparasi BYON |
| 2026-09-12 | 10 | Beranda (T07) lanjutan: grid aksi cepat + section header BYON-style di `app/(app)/dashboard/page.tsx`. Komitmen sesi: Beranda sebagai hub semua fitur + verifikasi build + pasang ke emulator. | Build + verifikasi di emulator + commit checkpoint |
